#ifndef SOLVER_H
#define SOLVER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "checks.h"
#include "loadGrid.h"

cCoords solver(char *word, char *filename);

#endif // SOLVER_H